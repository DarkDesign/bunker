import { Bunker } from '@/core/types';

export const descriptions: Bunker.IDescriptions[] = [
  {
    text: '',
    additionalCards: {},
    isAgeRestrictions: false
  }
].filter(item => item.text !== '')
